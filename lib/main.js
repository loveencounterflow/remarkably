// Generated by CoffeeScript 1.8.0
(function() {
  var TEXT, TRM, alert, badge, debug, echo, help, info, log, rpr, urge, warn, whisper;

  TRM = require('coffeenode-trm');

  rpr = TRM.rpr.bind(TRM);

  badge = 'REMARKABLY';

  log = TRM.get_logger('plain', badge);

  info = TRM.get_logger('info', badge);

  whisper = TRM.get_logger('whisper', badge);

  alert = TRM.get_logger('alert', badge);

  debug = TRM.get_logger('debug', badge);

  warn = TRM.get_logger('warn', badge);

  help = TRM.get_logger('help', badge);

  urge = TRM.get_logger('urge', badge);

  echo = TRM.echo.bind(TRM);

  TEXT = require('coffeenode-text');

  this.examples = require('./examples');

  this.main = function() {
    var MD, enable, html, settings, source;
    enable = 'full';
    settings = {
      html: true,
      xhtmlOut: false,
      breaks: false,
      langPrefix: 'language-',
      linkify: true,
      typographer: true,
      quotes: '“”‘’'
    };
    MD = new (require('remarkable'))(enable, settings);
    debug(this.video);
    debug(this.emphasis);
    debug(this.emphasis2);
    source = "This =is= 'awesome'(c): %[example movie](http://example.com)";

    /*
       *# helo world
    
      > Blockquotes become `<p>` tags
      > inside `<blockquote>` tags.
      >
      > Another
      > `<p>` within the same `<blockquote>`.
    
      H~2~O
    
      <!-- produces `<pre><code>...</code></pre>`: -->
      ```coffee
      f = ( x ) -> x * x
      g = ( x ) -> x + x
    
      ```
     */
    return info(html = MD.render(source));
  };

  if (module.parent == null) {
    this.main();
  }

}).call(this);
